import { AdharFormatPipe } from './adhar-format.pipe';

describe('AdharFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new AdharFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
